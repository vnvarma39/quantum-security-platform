# ─────────────────────────────────────────────────────────────
# Quantum Security Intelligence Platform (QSIP)
# Python Backend Requirements
# Python 3.11+
# ─────────────────────────────────────────────────────────────

# ── Web Framework ────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
httpx==0.27.0
pydantic==2.7.1
pydantic-settings==2.2.1

# ── Quantum Computing ────────────────────────────────────────
qiskit==1.1.0
qiskit-aer==0.14.2               # High-performance simulator
qiskit-ibm-runtime==0.23.0       # IBM Quantum hardware access
qiskit-optimization==0.6.1       # QAOA / QUBO utilities
qiskit-algorithms==0.3.0         # VQE, QAOA algorithm primitives

# AWS Braket (optional — uncomment to enable)
# amazon-braket-sdk==1.83.0
# amazon-braket-default-simulator-python==1.23.4

# ── Graph Analysis ───────────────────────────────────────────
networkx==3.3
pyvis==0.3.2                     # Graph visualization export
torch==2.3.0                     # PyTorch (CPU)
torch-geometric==2.5.3           # GNN for graph-level features
scipy==1.13.0
numpy==1.26.4

# ── Machine Learning / AI ────────────────────────────────────
scikit-learn==1.4.2
onnxruntime==1.18.0              # Patch classifier inference
transformers==4.41.0             # LLM-based patch explanation
sentence-transformers==3.0.0     # Semantic patch similarity

# ── Static Code Analysis ─────────────────────────────────────
libcst==1.3.2                    # Concrete syntax tree (Python)
tree-sitter==0.22.3              # Multi-language AST parsing
tree-sitter-c==0.21.4
tree-sitter-javascript==0.21.4
tree-sitter-go==0.21.2
bandit==1.7.8                    # Python security linter
semgrep==1.74.0                  # Multi-language SAST

# ── Fuzzing / Dynamic Testing ────────────────────────────────
atheris==2.3.0                   # Python fuzzing (libFuzzer)
hypothesis==6.100.2              # Property-based testing

# ── Vulnerability Data ───────────────────────────────────────
nvdlib==0.7.6                    # NVD / CVE data fetching
cvss==2.6                        # CVSS score parsing

# ── Cryptography / Security ──────────────────────────────────
cryptography==42.0.7
PyJWT==2.8.0

# ── Database / Storage ───────────────────────────────────────
sqlalchemy==2.0.30
alembic==1.13.1
redis==5.0.4
asyncpg==0.29.0                  # Async PostgreSQL

# ── Async / Concurrency ──────────────────────────────────────
anyio==4.4.0
aiofiles==23.2.1
celery==5.4.0                    # Background task queue
redis==5.0.4                     # Celery broker

# ── Observability ────────────────────────────────────────────
structlog==24.1.0
opentelemetry-sdk==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0
prometheus-client==0.20.0

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.1
typer==0.12.3                    # CLI tooling
rich==13.7.1                     # Terminal output
click==8.1.7
orjson==3.10.3                   # Fast JSON
arrow==1.3.0                     # Date/time utilities
deepdiff==7.0.1                  # Patch diff utilities

# ── Testing ──────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
pytest-mock==3.14.0
factory-boy==3.3.0
faker==25.2.0

# ── Dev / Linting (install with: pip install -r requirements-dev.txt) ──
# See requirements-dev.txt
